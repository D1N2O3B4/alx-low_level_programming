#include <stdio.h>
/**
 * main - reverse
 * Return: Always 0
 */
int main(void)
{
char alpharev[26] = "abcdefghijklmnopqrstuvwxyz";
for (int i = 26; i > 0; i--)
{
putchar(alpharev[i]);
}
putchar('\n');
return (0);
}
